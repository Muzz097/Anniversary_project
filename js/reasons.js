/* ==========================================
   29 REASONS WHY I LOVE YOU
   (Phase 07 — inside the Message card)
   ==========================================

   Not built yet. This will render 29 small
   cards (29 = Ailsa's birth date), opened
   one at a time after the main letter in
   Phase 06 has been read.

   Suggested shape:

   const reasons = [
       "The way you...",
       "Your smile...",
       // ...29 total
   ];

   document.addEventListener("DOMContentLoaded", () => {
       // build reason cards from the array above.
   });
*/
