/* ==========================================
   GLOBAL MUSIC PLAYER
   (Phase 12 — Our Playlist)
   ==========================================

   Not built yet. This will hold one shared
   <audio> instance so a song picked on the
   Playlist page keeps playing as the person
   moves between sections — instead of each
   section creating its own audio element.

   Planned shape:

   const player = new Audio();
   let currentTrackId = null;

   function playTrack(id, src) { ... }
   function stopAll() { ... }

   window.musicPlayer = { playTrack, stopAll };
*/
